# Current State

- Project: `mattamior/skills-hub`, a repository for developing and maintaining reusable Agent Skills for ChatGPT and Codex. Skills live under `skills/<skill-name>/` and use `SKILL.md` as their entry point.
- The developed reusable skills are `brand-design-system`, `canon-skill`, and `pet-avatar-generation`. Canon Skill's consolidated experimental delivery is tracked in PR #20; the earlier Day 1-3 draft PRs are superseded by that complete change, not separate runtime distributions.
- `canon-skill` 0.1.0 experimental contains a thin mode router; explicit begin/Prompt/Gen contracts; generic Subject Pack, authority, route/image-role, external-reference, evidence-planning, packet, validation/recovery, hook, transport and continuity contracts; six JSON Schemas; deterministic standard-library Python helpers; and anonymous Human/Pet/Virtual fixtures. It does not bundle an image model, real Subject Pack, visual-recognition implementation or provider-specific image client.
- The unchanged generic implementation at `a104a73b90a15bc5028cafad703da5915e803196` passed full Validate skills run `35501460375`, including 63 executable contract tests, schema/fixture checks, three explicitly simulated end-to-end paths, repository and upstream skill validators, installer, bilingual gallery and Chromium smoke tests. The final documentation/localization closure requires exact-revision CI and main verification before delivery is claimed.
- A real private consumer has a source-locked Subject Pack normalizer, scoped non-executing shadow planner and three deterministic subject-owned pixel handlers. Its 51 source/plan and synthetic-pixel tests passed private CI. Production cutover remains disabled; actual image transport, full independent legacy parity, visual acceptance, recorded conflict resolution and required approvals are not established by those tests.
- Canon Skill 0.2.0 and 1.0.0 exit criteria remain open. In particular, no second distinct real Subject Project has successful adoption evidence. Fixtures, synthetic bytes and supplied PASS observations are not real image/consumer acceptance. See `.agnir/evidence/2026-09-20-canon-experimental-closure.md` and bilingual `docs/canon-skill.*.md` for the exact boundary.
- `brand-design-system` remains identity-centric with conditional brand-foundation scoping, explicit assignment/concept/master/QA exit gates, recorded clearance and lifecycle governance, accessibility-aware implementation QA, language/script validation, and optional stable DTCG-compatible token or asset-manifest delivery. Standalone product UI component-library/design-system work remains out of scope.
- The public gallery is hosted on Cloudflare Pages at canonical `https://skills-hub.lapplax.com/`. The provider-assigned `https://skills-hub-ea7.pages.dev/` address remains the underlying endpoint. The superseded `skills-hub` Worker is retired. Former `https://skills-hub.hkooii.com/` is a legacy compatibility surface and is noindex when attached.
- Revision `61dc33acbf75171018b4e8f680f04ef0dff33c43` passed full validation, deployed successfully and passed exact-revision production acceptance on the canonical hostname. The custom domain is active, so the canonical-domain cutover and external CNAME prerequisite are complete. This is retained historical deployment evidence, not a claim that every later topic revision is already live.
- Migration experiment `3181f444ee94e35604e32c7ee2b66d3b9167709d` verified Pages access but no DNS-zone access: the DNS API returned HTTP 403 / Cloudflare `10000`. The canonical CNAME was provisioned outside the continuing deployment credential, preserving the Pages Edit-only boundary.
- The fully bilingual public catalog and `/skills/<skill-name>/` detail pages are repository-generated from `SKILL.md`, `agents/openai.yaml` and `locales/zh-CN.json`, without hand-maintained English metadata duplication or skill allowlists.
- Chinese mode covers homepage shell, loading/search/results, localized names/scope/invocations, complete operating contracts, detail-page chrome/copy states, document titles and branded 404. Technical identifiers and product/file names remain untranslated when translation would reduce precision.
- English `SKILL.md` is canonical for Agent instructions. Chinese `bodyMarkdown` is a display mirror validated for H2 count, non-code block count, code fences, relative references and explicit invocation. Formal README summaries/examples must match structured localization; READMEs are not parsed as catalog build data.
- The gallery is dependency-free runtime HTML/CSS/JavaScript with dense discovery-oriented hero/cards, repository/validation cues, keyboard-first search/live counts, responsive layout, visible focus and reduced-motion support.
- Runtime JavaScript/CSS uses deterministic SHA-256 content-version query tokens and `Cache-Control: no-cache`. Generated HTML and runtime assets advance together rather than retaining an old detail script against new markup.
- Catalog, skill details and 404 support persistent light/dark appearance. The default follows the OS; explicit light/dark choice is retained in local storage, with language-aware, keyboard-accessible controls.
- Build-time semantics use maintained `yaml` and `markdown-it` packages with raw Markdown HTML disabled. Catalog data is deterministic without wall-clock generation metadata; revision-bound deployment identity is in `health.json`.
- The build emits canonical sitemap/robots, security/indexing headers, Open Graph/Twitter metadata, a deterministic asset-version manifest and a bilingual noindex 404 page.
- Node dependencies are pinned by `package.json` and the npm lockfile; validation/deployment use `npm ci`. CI fixes Node `22.23.2` and Python `3.12.14`. Actions use immutable SHAs; deployment fixes Wrangler `4.131.2`. Dependabot proposes weekly npm and Actions updates.
- Repository validation uses `./scripts/validate-skills.py`, pinned Agent Skills `skills-ref` and pinned upstream skill-creator quick validation. CI discovers Python test modules, dynamically exercises the whole installer skill set, validates the complete gallery/localization/versioned assets, and runs real Chromium discovery/language/theme/clipboard/404 smoke tests.
- Production deployment uses an immutable validated revision. Acceptance compares deployed catalog data with the local build, checks matching revision and dynamic skill count in `health.json`, sitemap and security headers, requires noindex on provider/legacy surfaces and rejects noindex on the canonical domain.
- Legacy `.chatgpt/` continuity is retired. `AGNIR.yaml` and its `.agnir/` locators are the only repository-managed durable Project continuity. Agnir Core `0.1`, discovery profile `repository-filesystem/0.1`, operational package `v0.1.1` from immutable `e9712357ab590e5c1e5357b3cf3219d07d789aff` remain in use; Project identity and locators are preserved.
- `ZEROLOCAL.yaml` describes the active Pages runtime, canonical URL, Pages credential and external DNS trust boundary rather than the retired Worker.
- GitHub main lacks branch protection/rulesets. Enabling lightweight required validation and blocking destructive ref changes remains external governance because the connected capability cannot mutate rulesets. Stale remote-topic branch cleanup similarly awaits branch-ref deletion capability.
