{
  "name": "skills-hub-gallery",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-site.mjs && node scripts/localize-site.mjs",
    "check:site": "node scripts/verify-site.mjs",
    "check": "npm run build && npm run check:site"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
